<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE MISTER</title>
    <link rel="shortcut icon" href="/img/1Logo.png" type="image/x-icon"><title>Imagenes</title>
    <link rel="stylesheet" href="/stylesheets/admin.css">

</head>
<body>
    <header>
        <div class="logo">
            <img src="/img/Mister.png" alt="El logo de Mister" width="110px" height="90px">
        </div>
        <div class="nombre">
            <h1>THE MISTER</h1>
        </div>
        <nav>
            <ul>
                <li><a href="/home">Inicio</a></li>
                <li><a href="/categorias">Categorias</a></li>
                <li><a href="/productos">Productos</a></li>
                <li><a href="/imagenes">Imagenes</a></li>
            </ul>
        </nav>
    </header><br><br><br><br><br><br>
    <h1 class="img">Imagenes:  <%= imagenes.length %></h1><br>
    <div>
        <table border="1">
            <tr>
                <th>N°</th>
                <th>Imagen</th>
                <th>Destacado</th>
                <td><a href="/imgagg"><img src="/img/agregar.png" alt="agregar" width="30" height="30"></a></td>
            </tr>
            <tr>
                <% if (imagenes) { %>
                    <% for (let i = 0; i < imagenes.length; i++){  %>
                        <% if (imagenes) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><img src="<%= imagenes[i].url %>" width="300px" height="300px"></td>
                                <td><%= imagenes[i].destacado %></td>
                                <td> 
                                    <div class="input-group">
                                        <a href="/imgdelete/<%= imagenes[i].id%>" class="btn btn-danger"><img src="/img/delete.png" alt="borrar" width="30" height="30"></a>
                                        <a href="/imgedit/<%= imagenes[i].id%>" class="btn btn-info"><img src="/img/update.png" alt="actualizar" width="30" height="30"></a>
                                    </div>
                                </td>
                            </tr>    
                        <% } %>
                    <% } %>
                <% } else { %>
                        <tr>
                            <th>No hay imagenes en el sistema</th>
                        </tr>
                    <% }   %>
            </tr>
        </table>
    </div>
    <style>
        .img{
            font-size: 30px;
            font-family:'Times New Roman', Times, serif;
        }
    </style>
</body>
</html>