<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/admin.css">
    <title>THE MISTER</title>
    <link rel="shortcut icon" href="/img/1Logo.png" type="image/x-icon"><title>Productos</title>
</head>
<body>
    <header>
        <div class="logo">
            <img src="/img/Mister.png" alt="El logo de Mister" width="110px" height="90px">
        </div>
        <div class="nombre">
            <h1>THE MISTER</h1>
        </div>
        <nav>
            <ul>
                <li><a href="/home">Inicio</a></li>
                <li><a href="/categorias">Categorias</a></li>
                <li><a href="/productos">Productos</a></li>
                <li><a href="/imagenes">Imagenes</a></li>
            </ul>
        </nav>
    </header><br><br><br><br><br><br>
    <h1 class="prod">Productos: <%= productos.length %></h1><br>
    <div>
        <table border="1">
            <tr>
                <th>N°</th>
                <th>Codigo</th>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Descripcion</th>
                <td><a href="/prdagg"><img src="/img/agregar.png" alt="agregar" width="30" height="30"></a></td>
            </tr>
            <tr>
                <% if (productos) { %>
                    <% for (let i = 0; i < productos.length; i++){  %>
                        <% if (productos) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><%= productos[i].codigo %></td>
                                <td><%= productos[i].nombre %></td>
                                <td><%= productos[i].precio %>€</td>
                                <td><%= productos[i].marca %></td>
                                <td><%= productos[i].modelo %></td>
                                <td><%= productos[i].descripcion %></td>
                                <td> 
                                    <div class="input-group">
                                        <a href="/prddelete/<%= productos[i].id%>" class="btn btn-danger"><img src="/img/delete.png" alt="borrar" width="30" height="30"></a>
                                        <a href="/prdedit/<%= productos[i].id%>" class="btn btn-info"><img src="/img/update.png" alt="actualizar" width="30" height="30"></a>
                                    </div>
                                </td>
                            </tr>    
                        <% } %>
                    <% } %>
                <% } else { %>
                        <tr>
                            <th>No hay productos en el sistema</th>
                        </tr>
                    <% }   %>
            </tr>
        </table>
    </div>
    <style>
        .prod{
            font-size: 30px;
            font-family:'Times New Roman', Times, serif;
        }
    </style>
</body>
</html>